/**
 * A functional component that displays a loading message.
 *
 * @returns {JSX.Element} A JSX element containing the loading message.
 */
export default function Loading() {
    // Return a JSX element with the loading message.
    return <div>Loading users ...</div>
}